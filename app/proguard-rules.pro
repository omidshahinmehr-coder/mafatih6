# Add project specific ProGuard rules here.

# Model classes are only ever accessed directly by our own code (no
# reflection/serialization library reads their fields by name), so no extra
# -keep rules are needed for them; AppCompat/Material/RecyclerView ship their
# own consumer ProGuard rules automatically via their AARs.
